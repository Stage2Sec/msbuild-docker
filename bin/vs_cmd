#!/usr/bin/env bash

# abort the script if there is an error
set -eo pipefail

DIR="$(pwd | tr / \\\\)"

# spawn vs dev shell & execute commands
WINEDEBUG=-all wine cmd /c "/opt/msbuild/vs_buildtools/Common7/Tools/VsDevCmd.bat && cd /d $DIR && $*"
